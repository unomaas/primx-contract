// custom hook to take in an array of estimate object and return a mutated array with new keys based on the necessary math needed for all
// the displays
const useImperialEstimateCalculations = (estimateArray) => {
 
    
    return estimateArray;
}

export default useImperialEstimateCalculations;